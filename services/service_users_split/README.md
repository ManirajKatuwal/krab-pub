# service_users_split

Generated by `krab topology split users`.

## Included scaffold
- health/readiness endpoints
- protocol adapter stubs
- domain skeleton
- contract conformance placeholder tests

## Next actions
1. Move `users` domain logic behind contract traits in `krab_core`.
2. Implement local and remote adapters for each enabled protocol.
3. Wire topology selection through runtime config and CI matrix.
